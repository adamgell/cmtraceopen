//! Company Portal source parsers.
//!
//! Company Portal is a first-class Intune surface (sign-in, enrollment, app
//! catalog, compliance, sync, device actions, support) and is deliberately kept
//! separate from the IME and ESP pipelines even where the workflows overlap.

pub mod macos;
