[package]
name = "windows-file-identity-probe"
version = "0.0.0"
edition = "2021"

[dependencies]
windows = { version = "=0.62.2", features = ["Win32_Foundation", "Win32_Storage_FileSystem"] }

[workspace]
