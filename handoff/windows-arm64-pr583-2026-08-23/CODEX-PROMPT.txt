Open README.md and execute the Windows 11 ARM64 validation in order. Authenticate the transfer ZIP against the sender's out-of-band SHA-256 before executing it. Treat MANIFEST.json's source commit, tree, lockfile blobs, remote, branch, and Rust target as immutable. Before handling trusted bytes, require an exclusive clean disposable account/session; any unexpected same-account process, scheduled task, startup item, injected tool, sync client, or path mutation is a hard stop, with no further package/source execution and no revert before separate approval. Use only a new fixed-local-NTFS clone, evidence root, and disjoint return directory outside synchronized and reparse paths. Use a disposable account without credential, signing, proxy, SSH-agent, or package-registry state. Do not push, merge, reset, clean, sign, publish, upload raw evidence, use customer data, record returned identities, clear a real Event Log, or modify another checkout. Stop for prerequisite installation or elevation, VM snapshot/revert, installer execution, file-association changes, VSS mutation, second-machine access, custom-channel creation, clear execution, signing, or manual UI confirmation. Run local ignored Event Log tests individually, require a real target-local CMTRACE_EVTX_FIXTURE, use the exact private provider helper for the retained database, and complete the 68 structured manual rows without freeform returned text. Never treat a skipped, vacuous, blocked, remote-unavailable, polling-unobserved, unsigned, uninstalled, or adjacent-capability gate as PASS. Never use the exporter's ContractOnly developer mode as target evidence or a return. Return only the production archive produced and freshly verified by New-CMTraceOpenArm64ValidationReturn.ps1 plus its outer SHA-256 file, and deliver the literal outer SHA-256 independently through a trusted out-of-band channel so the receiver can compare both received files before extraction.
